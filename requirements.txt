# Qwen-Omni auf RunPod (GPU). Siehe README für transformers-Version.
torch>=2.1.0
accelerate>=0.30.0
# WICHTIG: transformers 5.x bricht 4-bit MoE (lädt ~31 GB FP16 → OOM auf 32 GB)
transformers>=4.51.0,<5.0.0
qwen-omni-utils>=0.0.8
soundfile>=0.12.0
pandas>=2.2.0
openpyxl>=3.1.2
python-dotenv>=1.0.0
psutil>=5.9.0
tqdm>=4.66.0

# Für 30B auf 32 GB GPU (OMNI_LOAD_IN_4BIT=1):
bitsandbytes>=0.43.0

# Optional (weniger VRAM, wenn installiert):
# pip install -U flash-attn --no-build-isolation
